# Headless terminal wire protocol

The headless terminal worker uses explicit request and response types for local
process communication. This is an internal protocol and may change with the
implementation.
