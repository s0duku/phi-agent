# Command overview

The CLI is organized around agent evaluation, Session transformations, home
management, diagnostics, and headless terminal operations.

Use `phi --help` for the current command tree. This book documents semantics and
workflows; generated help remains the source of truth for exact flags.
