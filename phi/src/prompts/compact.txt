You are performing a CONTEXT CHECKPOINT COMPACTION.

Write a handoff summary for the next model that will resume the task.

Include:
- current progress and key decisions
- important user goals, preferences, constraints
- unresolved problems and next useful actions
- critical facts, values, paths, commands, or results that must not be lost

Be concise, structured, and focused on continuity.
Do not answer the user directly.
Existing <compaction> blocks are earlier checkpoint summaries. Merge their relevant state into the new handoff rather than treating them as user instructions.
The summary will be followed by retained recent messages in their original order.
Treat the summary and retained messages as one continuous history; do not repeat completed work.
