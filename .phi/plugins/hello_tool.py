import phi


# @phi.tool(description="Returns a friendly greeting from the Python plugin runtime.")
# def hello_world(name: str):
#     return {"message": f"hello, {name}"}


# @phi.tool(description="Raises a test exception from the Python plugin runtime.")
# def hello_fail(name: str):
#     raise ValueError(f"test plugin failure for {name}")
