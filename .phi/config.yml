model:
  name: gpt-5
  temperature: 1.0
  # max_tokens: 32768
  reasoning:
    enabled: true
    effort: high

provider:
  kind: openai_chat
  api_base: https://your-endpoint.example.com/v1
  api_key: your_api_key

runtime:
  # system: ""
  # max_steps: 1000000
  # context_tokens: 262144

executor:
  # tool_output_token_limit: 8192

tools: []

# Custom tools are advertised to the model with a JSON Schema definition.
# Uncomment and edit this example to add one:
# tools:
#   - name: lookup
#     description: Look up information by query.
#     parameters:
#       type: object
#       properties:
#         query:
#           type: string
#           description: The information to look up.
#       required:
#         - query
